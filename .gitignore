# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code local state (never commit — machine/user-specific)
.claude/
.claude.json
.mcp.json.local

# Secrets — keep tokens out of git, use env vars instead
.env
.env.*
*.pem
*.key

# Logs
*.log
npm-debug.log*

# Archives (don't commit zipped copies of this repo)
*.zip